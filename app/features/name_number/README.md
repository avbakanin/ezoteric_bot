# Число имени

## Назначение
- Рассчитывает нумерологическое число имени на основе русских и латинских букв.
- Предлагает пользователю расшифровку с ключевыми качествами.

## Основные обработчики
- `router.py::name_number_command` — команда `/name_number`, запускает сценарий.
- `router.py::name_number_button` — текстовая кнопка «🔤 Число Имени».
- `router.py::handle_name_input` — принимает текст, валидирует и выводит результат.

## Логика
- Использует `calculate_name_number` и `get_name_number_description` из `app.shared.calculations`.
- После расчёта сбрасывает состояние и возвращает пользователя в меню.

