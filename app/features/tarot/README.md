# 🔮 Таро

## Назначение

Гадание на картах Таро с различными раскладами. Система позволяет получить ответы на вопросы и инсайты через интерпретацию карт с учетом их позиции в раскладе и положения (прямое/перевернутое).

## Возможности

- **78 карт**: 22 старших аркана + 56 младших арканов (4 масти × 14 карт)
- **7 раскладов**: от простых до сложных много-карточных
- **Вопрос перед раскладом**: можно задать вопрос для более точной интерпретации
- **История раскладов**: все расклады сохраняются для последующего просмотра
- **Кэширование**: карта дня и расклад на день сохраняются на сутки
- **Автоматическое определение контекста**: система анализирует вопрос и выбирает подходящий контекст (любовь, карьера, здоровье, общее)

## Бесплатные функции

### Доступные расклады:

1. **📅 Карта дня** (`single_card`)
   - Одна карта, которая описывает энергию дня
   - Кэшируется на сутки
   - Показывает основную тему и совет дня

2. **🎴 Расклад на три карты** (`three_card`)
   - Прошлое → Настоящее → Будущее
   - Классический расклад для понимания развития ситуации
   - Показывает, как прошлое влияет на настоящее и куда ведет будущее

3. **❓ Таро Да/Нет** (`yes_no`)
   - Быстрый ответ на вопрос
   - Используются только старшие арканы
   - Ответ: Да / Нет / Возможно с объяснением

4. **📅 Расклад на день** (`daily_three`)
   - Три карты: Утро → День → Вечер
   - Помогает планировать день
   - Кэшируется на сутки

### Особенности бесплатного режима:

- Все базовые расклады доступны
- Можно задавать вопросы перед раскладом
- История последних раскладов
- Разнообразные интерпретации для каждой карты

## Premium функции

### Дополнительные расклады:

1. **⛪ Кельтский крест** (`celtic_cross`)
   - 10 карт: полный анализ ситуации
   - Покрывает все аспекты: прошлое, настоящее, будущее, внутренние и внешние влияния
   - Самый популярный и детальный расклад Таро

2. **💑 Расклад на отношения** (`relationship`)
   - 5 карт: анализ отношений между двумя людьми
   - Показывает позиции обоих партнеров, динамику отношений, препятствия и будущее
   - Идеально для вопросов о любви и партнерстве

3. **💼 Расклад на карьеру** (`career`)
   - 6 карт: детальный анализ профессиональной ситуации
   - Сильные стороны, препятствия, скрытые возможности, рекомендации и будущее в карьере
   - Специализирован для вопросов о работе и финансах

### Преимущества Premium:

- Доступ ко всем расширенным раскладам
- Более глубокие интерпретации
- Приоритетная поддержка
- История без ограничений (более 100 раскладов)

## Основные обработчики

### Команды и кнопки:
- `router.py::tarot_command` — команда `/tarot`, открывает выбор раскладов
- `router.py::tarot_button` — текстовая кнопка «🔮 Таро» в главном меню
- `router.py::tarot_select_spread_callback` — возврат к выбору расклада
- `router.py::tarot_history_callback` — просмотр истории раскладов

### Выбор и выполнение расклада:
- `router.py::spread_callback` — обработчик выбора расклада, запрашивает вопрос (кроме Да/Нет)
- `router.py::tarot_question_skip` — пропуск вопроса, выполнение расклада без вопроса
- `router.py::handle_tarot_question` — обработка вопроса пользователя перед раскладом
- `router.py::_perform_spread` — основная функция выполнения расклада с интерпретацией

## Логика работы

### Загрузка данных:
- Все карты загружаются из `tarot-data/` при первом обращении
- Данные кэшируются в памяти для быстрого доступа
- Структура: старшие арканы (2 файла), младшие арканы по мастям (4 файла), расклады (1 файл)

### Выбор карт:
- Для каждого расклада случайно выбираются карты из всей колоды (78 карт)
- Для расклада Да/Нет используются только старшие арканы (22 карты)
- Каждая карта может быть прямой или перевернутой (50% вероятность)
- Карты не повторяются в рамках одного расклада

### Интерпретация:
- Для старших арканов доступны контексты: `general`, `love`, `career`, `health`
- Для младших арканов — общая интерпретация
- Контекст определяется автоматически из вопроса пользователя (если задан)
- Для каждой позиции в раскладе выбирается случайная интерпретация из доступных вариантов

### Кэширование:
- **Карта дня** (`single_card`): результат сохраняется на день, повторные запросы показывают тот же результат
- **Расклад на день** (`daily_three`): кэшируется на сутки
- Кэш автоматически сбрасывается при наступлении нового дня

### История:
- Все расклады сохраняются в истории пользователя
- Сохраняется: дата, расклад, вопрос (если был), карты, интерпретации
- Хранится последние 100 раскладов
- Доступна команда просмотра истории

## Структура данных

### Файлы с картами (`tarot-data/`):
- `tarot_major_01.json` — старшие арканы 0-11 (Шут, Маг, Жрица, Императрица, Император, Иерофант, Влюбленные, Колесница, Сила, Отшельник, Колесо Фортуны, Справедливость)
- `tarot_major_02.json` — старшие арканы 12-21 (Повешенный, Смерть, Умеренность, Дьявол, Башня, Звезда, Луна, Солнце, Суд, Мир)
- `tarot_wands.json` — Жезлы (14 карт: Туз-10, Паж, Рыцарь, Королева, Король)
- `tarot_cups.json` — Кубки (14 карт)
- `tarot_swords.json` — Мечи (14 карт)
- `tarot_pentacles.json` — Пентакли (14 карт)
- `tarot_spreads.json` — описание всех раскладов и их позиций

### Формат данных карты:

**Старшие арканы:**
```json
{
  "name": "Маг",
  "emoji": "🎩",
  "number": 1,
  "upright": {
    "general": [...],
    "love": [...],
    "career": [...],
    "health": [...]
  },
  "reversed": {
    "general": [...],
    "love": [...],
    "career": [...],
    "health": [...]
  }
}
```

**Младшие арканы:**
```json
{
  "name": "Туз Жезлов",
  "emoji": "🔥",
  "suit": "wands",
  "upright": [...],
  "reversed": [...]
}
```

## Примеры использования

### Бесплатный пользователь:
1. Выбирает "🔮 Таро" в меню
2. Видит 4 бесплатных расклада + кнопку Premium
3. Выбирает "📅 Карта дня"
4. Получает интерпретацию карты дня
5. Может просмотреть историю своих раскладов

### Premium пользователь:
1. Видит все 7 раскладов
2. Может выбрать "⛪ Кельтский крест"
3. Задает вопрос: "Что ожидает меня в отношениях?"
4. Получает детальный расклад на 10 карт с персонализированной интерпретацией
5. История сохраняется с полными деталями

## Интеграция с другими фичами

- **Натальные данные**: в будущем можно использовать для персонализации интерпретаций
- **Лунный планировщик**: карта дня может учитывать фазу Луны
- **Число судьбы**: можно добавлять нумерологические аспекты к интерпретациям

## Технические детали

- **Сервис**: `app/shared/tarot_service.py` — загрузка данных, выбор карт, интерпретация
- **Хранение**: результаты сохраняются в `user_storage` (кэш и история)
- **Состояния**: `UserStates.waiting_for_tarot_question` — ожидание вопроса перед раскладом
- **Клавиатуры**: `app/shared/keyboards/tarot.py` — клавиатуры для выбора раскладов
