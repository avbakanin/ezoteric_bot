# Число дня

## Назначение
- Показывает пользователю число дня и текстовый прогноз.
- Кеширует результат и позволяет премиум-пользователям получать расширенный текст.

## Основные обработчики
- `router.py::_send_daily_number` — общая функция для команд и callback, проверяет подписку, кеш и выдаёт текст.
- `router.py::daily_number_command` — команда `/daily_number`.
- `router.py::daily_number_button` — текстовая кнопка «🌞 Число Дня».
- `router.py::daily_number_callback` — callback-кнопка из меню.

## Логика
- Проверяет активную подписку (`user_storage`) и кеш (`daily_number`).
- Тексты берутся из `numbers.json` с приоритетом блока `premium_daily`.
- Для бесплатных пользователей показывает CTA `get_premium_info_keyboard`.

