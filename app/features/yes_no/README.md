# Гадание «Да/Нет»

## Назначение
- Отвечает на вопросы пользователя в формате «Да / Скорее нет / Нет».
- Ограничивает количество запросов для бесплатных пользователей через дневной лимит.

## Основные обработчики
- `router.py::yes_no_command` — команда `/yes_no`, запускает сценарий и спрашивает вопрос.
- `router.py::yes_no_button` — текстовая кнопка «🔮 Гадание Да/Нет».
- `router.py::handle_yes_no_question` — принимает текст вопроса, проверяет лимит и выдаёт ответ.

## Логика
- Ответ формируется функцией `get_yes_no_answer` (через `format_yes_no_response`).
- Лимиты контролирует `user_storage.can_make_request` с типом `daily_requests`.

