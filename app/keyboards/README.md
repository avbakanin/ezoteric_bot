# Структура клавиатур бота

Модуль `keyboards` организован по функциональным группам для улучшения поддержки и масштабируемости проекта.

## Структура модулей

### 📁 `main.py` - Главное меню
Содержит клавиатуру главного меню бота с основными функциями.
- `get_main_menu_keyboard()` - Reply клавиатура главного меню

### 📁 `navigation.py` - Навигация
Клавиатуры для навигации по боту.
- `get_back_to_main_keyboard()` - Кнопка возврата в главное меню

### 📁 `results.py` - Результаты расчетов
Клавиатуры для отображения результатов нумерологических расчетов.
- `get_result_keyboard()` - Клавиатура для результата расчета числа судьбы
- `get_compatibility_result_keyboard()` - Клавиатура для результата совместимости

### 📁 `profile.py` - Профиль пользователя
Клавиатуры для страницы профиля.
- `get_profile_keyboard(has_calculated)` - Клавиатура профиля с опциями расчета

### 📁 `about.py` - Информация о боте
Клавиатуры для информационных страниц.
- `get_about_keyboard()` - Клавиатура страницы "О боте"

### 📁 `premium.py` - Premium функции
Клавиатуры для premium подписки и функций.
- `get_premium_info_keyboard()` - Информация о Premium

### 📁 `feedback.py` - Обратная связь
Клавиатуры для отзывов и обратной связи.
- `get_feedback_keyboard()` - Клавиатура отзывов с разными категориями

### 📁 `common.py` - Общие клавиатуры
Переиспользуемые клавиатуры общего назначения.
- `get_yes_no_keyboard()` - Клавиатура с кнопками Да/Нет

## Использование

Все клавиатуры доступны через главный модуль:

```python
from keyboards import (
    get_main_menu_keyboard,
    get_back_to_main_keyboard,
    get_result_keyboard,
    get_compatibility_result_keyboard,
    get_profile_keyboard,
    get_about_keyboard,
    get_premium_info_keyboard,
    get_feedback_keyboard,
    get_yes_no_keyboard,
)
```

## Преимущества новой структуры

1. **Модульность** - Каждая группа клавиатур в отдельном файле
2. **Легкость поддержки** - Легко найти и изменить нужную клавиатуру
3. **Расширяемость** - Простое добавление новых клавиатур
4. **Организация** - Логическая группировка по функциональности
5. **Переиспользование** - Общие клавиатуры вынесены отдельно

## Добавление новой клавиатуры

1. Определите к какой группе относится клавиатура
2. Добавьте функцию в соответствующий модуль
3. Экспортируйте функцию в `__init__.py`
4. Используйте в обработчиках через импорт из `keyboards`

